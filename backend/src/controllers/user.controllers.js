import { asyncHandler } from "../utils/asyncHandler";



const userRegistration = asyncHandler(async(req, res)=>{
    // check ulplicate user 
    // fatch all data form UI
    // check user existing
    // saver user profile
    

})


export {userRegistration}